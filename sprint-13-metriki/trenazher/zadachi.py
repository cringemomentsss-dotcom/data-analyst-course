"""
ТРЕНАЖЁР СПРИНТА 13. 18 задач на метрики, воронки и когорты.

Спринт идёт вглубь, а не вширь. Формула DAU помещается в одну строку;
трудное здесь — определения. Что считается активностью. Какой из трёх
retention ты посчитал. Открытая у тебя воронка или закрытая. За какой
горизонт LTV. Половина задач ровно об этом: одни и те же данные дают
разные числа, и ошибка не в коде, а в том, что определение не названо.

Проверка:    make test SPRINT=13
Одна задача: python3 -m pytest sprint-13-metriki/trenazher -q -k retention
"""

import numpy as np
import pandas as pd


# === Часть 1. Активность ===================================================

def dau_wau_mau(sessions, user_col="user_id", ts_col="ts"):
    """DAU, WAU, MAU и два sticky factor.

    Возвращает {'dau', 'wau', 'mau', 'sticky_dau_mau', 'sticky_dau_wau'}.

    DAU — среднее число уникальных пользователей за день, WAU — за
    неделю, MAU — за месяц. Считается по уникальным пользователям
    внутри периода, а не суммой дневных значений: человек, заходивший
    все 30 дней, в MAU входит один раз.

    Недели бери через dt.to_period('W'), месяцы через dt.to_period('M').

    Sticky factor DAU/MAU показывает, какую долю месячной аудитории
    ты видишь в среднестатистический день. 0.5 — мессенджер, 0.1 —
    сервис, которым пользуются раз в неделю. Само по себе число ничего
    не значит: сравнивать можно только с собой во времени.

    Ловушка, которую эта реализация НЕ лечит: среднее берётся по дням,
    ПРИСУТСТВУЮЩИМ в данных. День без единой сессии в среднее не войдёт,
    и DAU окажется завышен. На боевых данных сначала делают календарь
    дат и приклеивают к нему активность. В тесте это видно на примере.
    """
    raise NotImplementedError


def active_users(events, day, window=1, user_col="user_id", ts_col="ts",
                 name_col=None, active_events=None):
    """Активные пользователи за окно, заканчивающееся днём day включительно.

    Возвращает {'day', 'window', 'users', 'events'}.
    В 'day' — объект datetime.date, в 'window' — целое число дней.

    window=1 даёт DAU за конкретный день, window=7 — WAU по скользящему
    окну, window=30 — MAU. Окно закрытое с обеих сторон: [day-window+1, day].

    active_events — список событий, которые считаются активностью.
    Если он не задан, активностью считается любое событие. Если задан,
    а name_col нет — подними ValueError.

    Зачем этот параметр: «активность» — не свойство данных, а решение
    аналитика. Открыл приложение, посмотрел ленту, сделал заказ — это
    три разных DAU, отличающихся в разы. Метрика без явного определения
    активности — не метрика, а число.
    """
    raise NotImplementedError


# === Часть 2. Retention и когорты ==========================================
#
# Три определения retention ниже дают три разных числа на одних данных.
# Это не ошибка ни в одном из них. Ошибка — назвать результат «retention»
# и не сказать, какое определение использовано.


def retention_nday(users, events, n, user_col="user_id",
                   reg_col="reg_date", ts_col="ts"):
    """N-day retention: активность РОВНО на день N после регистрации.

    Возвращает {'n', 'cohort_size', 'retained', 'retention'}.

    День N считается от даты регистрации, обе даты приводятся к суткам
    (нормализуются). День 0 — день регистрации.

    Знаменатель — все пользователи из users, а не только те, у кого
    есть события. Пользователь, не сделавший ничего, — это не пропуск
    в данных, это ушедший пользователь, и он обязан попасть в знаменатель.

    Определение строгое и потому пессимистичное: человек, заходивший на
    дни 6 и 8, в 7-day retention не попадёт. Стандарт мобильных игр.
    """
    raise NotImplementedError


def rolling_retention(users, events, n, user_col="user_id",
                      reg_col="reg_date", ts_col="ts"):
    """Rolling retention: активность на день N ИЛИ ПОЗЖЕ.

    Возвращает тот же словарь, что retention_nday.

    Самое оптимистичное из трёх определений и единственное монотонное:
    rolling(1) >= rolling(7) >= rolling(30) всегда.

    Отвечает на вопрос «сколько пользователей дожили до дня N», а не
    «сколько были активны в этот день». Хорош для оценки жизненного
    цикла, плох для мониторинга: у него есть right-censoring — вчерашняя
    когорта физически не могла дожить до дня 30, и её rolling(30) равен
    нулю не потому, что она плохая.
    """
    raise NotImplementedError


def unbounded_retention(users, events, n, user_col="user_id",
                        reg_col="reg_date", ts_col="ts"):
    """Unbounded (bracket) retention: активность в интервале [1, N].

    Возвращает тот же словарь, что retention_nday.

    День 0 исключён намеренно: в день регистрации активны почти все,
    и включение нулевого дня превратило бы метрику в константу 100%.

    Это определение чаще всего имеют в виду продакты, когда говорят
    «недельный retention»: вернулся ли человек хоть раз за неделю.
    """
    raise NotImplementedError


def retention_curve(users, events, max_day=14, user_col="user_id",
                    reg_col="reg_date", ts_col="ts"):
    """Кривая удержания по N-day определению, дни от 0 до max_day.

    Возвращает DataFrame со столбцами day_n, retained, retention;
    строка на каждый день диапазона, включая дни с нулевым удержанием.

    Пропущенные дни возвращать нулями, а не выбрасывать. Дырявая кривая
    на графике выглядит как плато, которого нет.

    Читается кривая по двум признакам: как быстро падает начало и на
    какой высоте выходит на плато. Плато — это ядро продукта. Продукт
    без плато (кривая уходит в ноль) не растёт: сколько ни лей в
    воронку, оно вытечет.
    """
    raise NotImplementedError


def cohort_matrix(users, events, max_period=6, freq="M", user_col="user_id",
                  reg_col="reg_date", ts_col="ts"):
    """Retention-матрица: строки — когорты, столбцы — номер периода.

    Возвращает DataFrame: индекс — период регистрации (pd.Period),
    столбцы — целые числа от 0 до max_period, значения — процент
    удержания, округлённый до 0.1.

    Когорта — пользователи, зарегистрировавшиеся в один период.
    Номер периода — расстояние от периода регистрации до периода
    активности в тех же единицах (freq='M' — месяцы, 'W' — недели).

    Когорта без единого события обязана дать строку из нулей, а не
    строку из NaN и не исчезнуть из матрицы. Полностью мёртвая когорта —
    самый ценный сигнал в этой таблице, и терять его нельзя.

    Читается матрица по столбцам, а не по строкам: сравнение периода 1
    у разных когорт показывает, улучшается ли продукт со временем.
    """
    raise NotImplementedError


# === Часть 3. Воронки ======================================================

def funnel_open(events, steps, user_col="user_id", name_col="event_name"):
    """Открытая воронка: уникальные пользователи на каждом шаге без учёта порядка.

    Возвращает DataFrame со столбцами step, users, step_cr, from_top.
    step_cr — конверсия из предыдущего шага в процентах, округление до
    сотых; у первого шага None. from_top — конверсия от первого шага.

    Шаг засчитывается, если событие было когда-нибудь. Пользователь,
    сделавший заказ, но не прошедший регистрацию (например, оформивший
    его в приложении, а событие регистрации потерялось), попадёт в
    заказы, минуя регистрацию.

    Отсюда фирменный признак сломанной аналитики: конверсия шага больше
    100%. На открытой воронке это нормально и означает ровно то, что
    порядок не проверялся.
    """
    raise NotImplementedError


def funnel_closed(events, steps, user_col="user_id", name_col="event_name",
                  ts_col="ts"):
    """Закрытая воронка: шаг засчитывается только если предыдущие были РАНЬШЕ.

    Возвращает DataFrame с теми же столбцами, что funnel_open.

    Для каждого пользователя берётся ПЕРВОЕ время каждого события.
    Пользователь остаётся в воронке, пока его времена не нарушают
    порядок шагов; выпав один раз, он не возвращается.

    Шаг, которого нет в данных вообще, обязан дать 0, а не исключение.
    Это не редкость: разрез по платформе легко даёт сегмент, где до
    последнего шага не дошёл никто.

    Закрытая воронка всегда даёт числа не больше открытой. Расхождение
    между ними — не погрешность, а измерение: оно показывает, сколько
    пользователей идут не тем путём, который нарисовал продакт.
    """
    raise NotImplementedError


def funnel_by_segment(events, steps, segment_col, user_col="user_id",
                      name_col="event_name", ts_col="ts"):
    """Закрытая воронка в разрезе сегмента.

    Возвращает длинный DataFrame: segment, step, users, step_cr, from_top.
    Сегменты идут по возрастанию, внутри сегмента — в порядке steps.
    Пустые сегменты (NaN в segment_col) пропускаются.

    Зачем: общая воронка показывает средний по больнице результат.
    Настоящее узкое место почти всегда виден только в разрезе — падает
    один сегмент, а в сумме это выглядит как равномерная просадка.

    Правило поиска узкого места: смотреть не на самый низкий процент,
    а на самый большой абсолютный отвал, помноженный на реалистичный
    потенциал улучшения. Шаг с конверсией 5% может быть нормой отрасли,
    а шаг с 60% против 85% у соседнего сегмента — это потерянные деньги.
    """
    raise NotImplementedError


def time_to_convert(events, step_from, step_to, user_col="user_id",
                    name_col="event_name", ts_col="ts"):
    """Время между двумя шагами в часах.

    Возвращает {'n', 'median_hours', 'mean_hours', 'p90_hours'}
    или None, если ни один пользователь не прошёл оба шага.

    Берётся первое время каждого шага. Пользователи, у которых второй
    шаг раньше первого, отбрасываются: это не отрицательное время, это
    другой сценарий.

    Зачем метрика: она задаёт горизонт окна атрибуции. Если медиана
    конверсии — три дня, то воронка, посчитанная за вчера, недосчитает
    половину, и «падение конверсии» окажется артефактом окна.

    Медиана и среднее здесь расходятся всегда: распределение времени
    до конверсии с длинным хвостом. Отчитываются медианой и p90.
    """
    raise NotImplementedError


# === Часть 4. Деньги =======================================================

def arpu_arppu(n_users, orders, revenue_col="revenue", user_col="user_id"):
    """ARPU, ARPPU, доля платящих и средний чек.

    Возвращает {'revenue', 'users', 'payers', 'arpu', 'arppu',
    'payer_share', 'avg_order'}.

    ARPU — выручка на всех пользователей, ARPPU — только на платящих.
    n_users передаётся отдельно, потому что в orders неплатящих нет,
    а знаменатель ARPU обязан их включать.

    Тождество, которое стоит держать в голове:
        ARPU = ARPPU × доля платящих.
    Оно даёт бесплатный санити-чек и сразу говорит, куда смотреть:
    ARPU вырос — это платящие стали платить больше или платящих стало
    больше? Это два разных вывода и два разных действия.

    avg_order — на заказ, а не на пользователя. Путать avg_order и
    ARPPU — самая частая ошибка в отчётах по деньгам.
    """
    raise NotImplementedError


def avg_check(orders, revenue_col="revenue"):
    """Средний чек: среднее, медиана и оценка перекоса хвостом.

    Возвращает {'n', 'mean', 'median', 'p90', 'skew_ratio',
    'top1pct_revenue_share'} или None на пустых данных.

    skew_ratio = mean / median. Единица — симметричное распределение,
    два и выше — среднее тянет хвост и отчитываться им нельзя.

    top1pct_revenue_share — доля выручки, которую дал верхний 1%
    заказов; размер верхушки округляется до ближайшего целого, но не
    меньше одного заказа. Нечисловые значения отбрасываются.

    Зачем: «средний чек вырос на 12%» после прихода одного корпоративного
    клиента — это не рост, это один клиент. Медиана его не заметит, и
    в этом смысл пары.
    """
    raise NotImplementedError


def ggr_ngr(bets, bet_col="bet", win_col="win", bonus_col=None, fees=0.0):
    """GGR и NGR — выручка в гемблинге и любом бизнесе на ставке.

    Возвращает {'turnover', 'payout', 'ggr', 'bonus', 'fees', 'ngr',
    'hold_pct', 'rtp_pct'}.

    GGR = оборот − выплаты. NGR = GGR − бонусы − комиссии.
    hold_pct = GGR / оборот × 100, rtp_pct = выплаты / оборот × 100;
    вместе они дают 100%.

    bonus_col необязателен: если он не передан или такого столбца нет,
    бонусы считаются нулевыми. Пропуски в суммах трактуются как нули.

    Почему это отдельная задача, а не «выручка»: оборот в гемблинге на
    порядок больше выручки, и человек со стороны, увидев turnover в
    отчёте, решит, что бизнес в десять раз крупнее. NGR — то, из чего
    платят зарплату; GGR — то, что показывают в презентациях.
    """
    raise NotImplementedError


def ltv_by_cohort(users, orders, max_period=6, freq="M", user_col="user_id",
                  reg_col="reg_date", ts_col="ts", revenue_col="revenue"):
    """Накопленный LTV по когортам: сколько принёс пользователь к периоду N.

    Возвращает DataFrame: индекс — когорта (pd.Period), столбцы —
    от 0 до max_period, значения — накопленная выручка на одного
    пользователя когорты, округление до сотых.

    Знаменатель — размер когорты целиком, включая тех, кто не заплатил
    никогда. Делить на платящих — это ARPPU, а не LTV.

    Значения накапливаются: столбец N — сумма периодов от 0 до N.
    Когорта без заказов даёт строку нулей, а не NaN: накопительная
    сумма по пустой строке — классический источник NaN, и его надо
    закрыть явно.

    Главное правило: LTV без горизонта — бессмысленное число. «LTV
    равен 40 евро» ничего не значит; «LTV за 3 месяца равен 40 евро»
    можно сравнить с CAC. Столбцы этой матрицы и есть горизонты.
    """
    raise NotImplementedError


def unit_economics(cac, arpu, margin=1.0):
    """LTV/CAC и срок окупаемости.

    Возвращает {'ltv', 'cac', 'ltv_cac', 'payback_periods', 'profitable'}.

    LTV = arpu × margin, где margin — доля маржи в выручке. Считать
    юнит-экономику по выручке, а не по марже, — способ доказать
    прибыльность бизнеса, который теряет деньги на каждом заказе.

    payback_periods = cac / ltv — за сколько периодов ARPU окупится
    привлечение. При cac = 0 верни ltv_cac и payback_periods равными
    None, а profitable — True: делить на ноль нельзя, а органика
    прибыльна по определению.

    Ориентир индустрии LTV/CAC = 3 — это ориентир, а не закон. Он
    имеет смысл только когда LTV взят на горизонте, сопоставимом со
    сроком, на который бизнес может занять деньги.
    """
    raise NotImplementedError


# === Часть 5. Контроль =====================================================

def sanity_check(parts, total, tol=0.01):
    """Сходимость суммы по разрезам с общим итогом.

    Возвращает {'sum_parts', 'total', 'diff', 'ok'}.
    diff = сумма частей − итог. Допуск tol — ОТНОСИТЕЛЬНЫЙ: расхождение
    считается допустимым, если |diff| <= tol × max(1, |total|).

    Первое, что делают с готовым отчётом. Сумма по каналам не сошлась с
    общим итогом — значит часть строк потерялась в джойне, или у части
    канал NULL и он выпал из группировки, или один пользователь попал
    в два канала и посчитан дважды. Все три случая меняют вывод.
    """
    raise NotImplementedError


def metric_consistency(report, tol=1e-9):
    """Проверка внутренней непротиворечивости отчёта по метрикам.

    На входе словарь, ключи необязательны: users, payers, arpu, arppu,
    revenue, conversion, dau, wau, mau. Отсутствующие ключи не проверяются.

    Возвращает {'ok', 'problems'} — список текстовых описаний нарушений
    в порядке проверки. Проверяй ровно эти шесть соотношений и называй
    их ровно этими строками:

        'payers > users'
        'arppu < arpu'
        'conversion вне [0, 1]'
        'dau > mau'
        'dau > wau'
        'wau > mau'
        'revenue != arpu * users'

    Для revenue допуск относительный: max(tol, 1e-6 × |revenue|).

    Зачем это отдельно от sanity_check: суммы могут сходиться идеально,
    а отчёт при этом быть невозможным. ARPPU ниже ARPU означает, что
    в знаменателе ARPU оказались не все пользователи. DAU выше MAU —
    что уникальность считалась по-разному в двух запросах. Такие вещи
    ловятся не сверкой итогов, а проверкой того, что числа вообще
    могут сосуществовать.
    """
    raise NotImplementedError
