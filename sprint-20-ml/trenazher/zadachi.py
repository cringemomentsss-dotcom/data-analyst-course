"""
ТРЕНАЖЁР СПРИНТА 20. 18 задач: постановка, утечка, валидация, метрики.

Здесь настоящий scikit-learn — в отличие от спринтов 17–19, где Spark и
Airflow были не нужны. Но задачи всё равно не про то, как вызвать fit:
это одна строка. Они про то, что происходит ДО и ПОСЛЕ неё — где взялась
целевая переменная, почему у модели AUC 0.99 и что означает выбранный порог.

Проверка:    make test SPRINT=20
Одна задача: python3 -m pytest sprint-20-ml/trenazher -q -k leak
"""

import numpy as np
import pandas as pd
from sklearn.compose import ColumnTransformer
from sklearn.impute import SimpleImputer
from sklearn.metrics import (accuracy_score, average_precision_score, f1_score,
                             mean_absolute_error, precision_score, r2_score,
                             recall_score, roc_auc_score)
from sklearn.model_selection import StratifiedKFold, cross_val_score
from sklearn.pipeline import Pipeline
from sklearn.preprocessing import OneHotEncoder, StandardScaler

SCALE_SENSITIVE = {"logreg", "svm", "knn", "kmeans", "pca", "ridge", "lasso",
                   "neural_net"}
SCALE_FREE = {"tree", "random_forest", "gradient_boosting", "xgboost",
              "lightgbm", "catboost", "naive_bayes"}


# === Часть 1. Постановка ===================================================

def define_target(events, users, event_col="event", ts_col="ts",
                  user_col="user_id", reg_col="reg_date",
                  target_event="deposit", window_days=30):
    """Целевая переменная с явным окном наблюдения.

    Возвращает {'table', 'n', 'positives', 'rate', 'window_days',
    'definition'}. В table — user_id, reg_date и y; строка на
    пользователя, порядок как в users.

    y равен единице, когда целевое событие произошло в окне
    [reg_date, reg_date + window_days). Интервал закрыт слева и открыт
    справа. Пользователь без событий получает ноль.

    **Определение целевой переменной — самый частый источник провала
    ML-проекта**, и провал этот тихий. «Предсказать отток» — не
    постановка: отток чего, за какое окно, от какого момента считаем.
    Модель, обученная на одном определении, а применяемая по другому,
    выдаёт правдоподобные числа, которые ничего не значат.

    Окно обязано быть в определении, а не подразумеваться.
    """
    raise NotImplementedError


def check_observation_window(users, data_end, reg_col="reg_date",
                             window_days=30):
    """Кому окно наблюдения ещё не закрылось.

    Возвращает {'total', 'cutoff', 'incomplete', 'usable',
    'share_incomplete', 'advice'}. cutoff — data_end минус window_days;
    incomplete — зарегистрировавшиеся строго позже cutoff.

    Пользователь, зарегистрировавшийся за пять дней до конца данных, ещё
    мог совершить целевое действие на тридцатый. Его ноль означает «ещё
    не успел», а не «не сделает».

    Такие строки не размечают нулём — их **исключают из выборки**. Иначе
    модель учится на систематически неверных метках, и учится тем хуже,
    чем свежее данные. Это тот же правый нижний треугольник, что в
    когортных матрицах спринта 13.
    """
    raise NotImplementedError


def baseline_scores(y_true, rule_pred=None):
    """Метрики тупых базовых решений.

    Возвращает {'positive_rate', 'constant', 'note'} и, если передан
    rule_pred, ещё 'rule'.

    В 'constant': roc_auc равен 0.5 по определению, pr_auc — средняя
    точность константного прогноза (доля класса), accuracy_all_zero —
    доля нулей.
    В 'rule': roc_auc, pr_auc, accuracy, precision, recall.

    **Baseline строят до модели, а не после.** Без него непонятно,
    хороша ли модель: AUC 0.72 — это прекрасно, если правило в одну
    строку даёт 0.55, и позорно, если оно даёт 0.71.

    Правило в одну строку часто оказывается почти так же хорошо, как
    градиентный бустинг, и в этом случае в прод идёт правило: его
    понимает бизнес, его не нужно переобучать, оно не ломается.
    """
    raise NotImplementedError


# === Часть 2. Утечка и разделение ==========================================

def leak_scan(df, target_col, feature_cols=None, corr_threshold=0.95,
              auc_threshold=0.95, state_suffixes=("_status", "_flag", "is_",
                                                  "_level", "_total", "_ever")):
    """Поиск признаков, подозрительных на утечку целевой переменной.

    Возвращает {'findings', 'n', 'clean', 'suspicious'}, где findings —
    [{'feature', 'reason', 'value'}] в порядке столбцов.

    Три проверки на признак, первое срабатывание останавливает разбор
    этого столбца:

    1. числовой признак с модулем корреляции не меньше corr_threshold;
    2. числовой признак, у которого одиночный ROC-AUC (взятый как
       max(auc, 1-auc), чтобы поймать и обратную связь) не меньше
       auc_threshold;
    3. имя начинается или кончается на что-то из state_suffixes —
       похоже на поле-состояние без отметки времени.

    Константные числовые столбцы пропускаются.

    Третья проверка — про самую коварную утечку. `is_verified` не
    коррелирует с целью идеально, но проставляется в момент депозита.
    В обучающих данных он есть, а **в момент прогноза его значение
    неизвестно** — ты предсказываешь для человека, который ещё ничего
    не сделал. Правило: признак-состояние без отметки времени
    использовать нельзя, пока не доказано, когда он выставляется.
    """
    raise NotImplementedError


def time_split(df, time_col, test_size=0.25):
    """Разделение по времени: учимся на прошлом, проверяемся на будущем.

    Возвращает {'train', 'test', 'n_train', 'n_test', 'boundary',
    'reason'}. Данные сортируются по времени, первые (1 - test_size)
    идут в train.

    **Случайное разделение на данных со временем — ошибка.** Модель
    увидит в обучении объекты, которых в момент прогноза ещё не
    существовало, и покажет качество, которого в проде не будет.

    Правило простое: если у данных есть время и модель будет применяться
    к будущему — разделяй по времени.
    """
    raise NotImplementedError


def split_sanity(train, test, id_col, target_col, time_col=None, tol=0.1):
    """Проверка корректности разделения.

    Возвращает {'overlap', 'rate_train', 'rate_test', 'time_leak',
    'problems', 'ok'}. Проверки:

    - один объект попал в обе части;
    - доли положительного класса разошлись больше чем на tol
      (относительно доли в train);
    - в тесте только один класс — метрики не посчитать;
    - при заданном time_col: максимум времени в train позже минимума
      в test.

    Первый пункт кажется невозможным, пока не начинаешь собирать
    признаки джойнами: дубль по ключу — и один пользователь в обеих
    частях.
    """
    raise NotImplementedError


# === Часть 3. Признаки =====================================================

def encode_categorical(values, method="onehot", target=None):
    """Кодирование категориального признака: 'onehot', 'label', 'target'.

    Пропуски заменяются строкой '__NA__'. Неизвестный метод — ValueError.
    Для 'target' отсутствие target — ValueError.

    | метод | что возвращает |
    |---|---|
    | onehot | 'frame' (DataFrame с префиксом 'c'), 'n_columns' |
    | label | 'codes', 'mapping' (категории по алфавиту) |
    | target | 'codes', 'mapping' (средний таргет по категории) |

    Плюс 'method' и 'warning' у всех.

    Что писать в warning:
    - onehot: при более чем 20 столбцах — про большую кардинальность,
      иначе пустая строка;
    - label: создаёт ложный порядок, годится деревьям, вреден линейным;
    - target: **посчитан на всей выборке, это утечка**; считать надо
      внутри фолдов.
    """
    raise NotImplementedError


def target_encoding_leak(values, target, n_splits=5, seed=0):
    """Насколько target encoding без кросс-валидации завышает качество.

    Возвращает {'auc_naive', 'auc_out_of_fold', 'inflation', 'leaks'}.

    auc_naive — AUC кодировки, посчитанной по всей выборке.
    auc_out_of_fold — AUC кодировки, где значение для каждого объекта
    посчитано **по остальным фолдам** (StratifiedKFold с shuffle и
    random_state=seed). Категории, не встретившиеся в обучающих фолдах,
    заполняются общим средним.
    leaks — разница больше 0.02.

    Target encoding — самый мощный способ закодировать категорию с
    большой кардинальностью и самый лёгкий способ обмануть себя.
    Посчитанный на всей выборке, он вносит в признак информацию о
    целевой переменной того же объекта — и модель показывает качество,
    которого нет.
    """
    raise NotImplementedError


def needs_scaling(model_kind):
    """Нужно ли масштабировать признаки. Множества SCALE_* даны выше.

    Возвращает {'model', 'needs_scaling', 'reason'}; неизвестная
    модель — ValueError.

    Линейные модели, метод опорных векторов, ближайшие соседи,
    K-Means и метод главных компонент опираются на расстояния или на
    величину коэффициентов: признак в рублях задавит признак в долях.

    Деревьям масштаб безразличен: дерево сравнивает значения внутри
    одного признака, а не между разными.

    Отсюда практическое следствие для пайплайна: масштабирование
    ставится **внутрь** пайплайна, а не применяется к данным заранее.
    Иначе оно посчитается по всей выборке, включая тестовую часть.
    """
    raise NotImplementedError


def missing_strategy(series, name="", threshold_drop=0.6, threshold_flag=0.05):
    """Что делать с пропусками в признаке.

    Возвращает {'feature', 'missing_share', 'strategy', 'add_flag',
    'reason'}.

    | доля пропусков | стратегия |
    |---|---|
    | 0 | 'ничего' |
    | не меньше threshold_drop | 'выбросить признак' |
    | иначе, числовой | 'медиана + флаг пропуска' |
    | иначе, категориальный | 'отдельная категория' |

    add_flag истинно, когда доля в [threshold_flag, threshold_drop).

    Про флаг пропуска стоит подумать отдельно. Сам факт, что значения
    нет, часто информативнее любого заполнения: пустой возраст может
    означать, что человек не заполнил профиль, а это отдельный тип
    поведения. Заполнив медианой и не поставив флаг, ты эту информацию
    выбрасываешь.
    """
    raise NotImplementedError


# === Часть 4. Метрики ======================================================

def classification_metrics(y_true, y_proba, threshold=0.5):
    """Полный набор метрик классификации при заданном пороге.

    Возвращает {'threshold', 'tp', 'fp', 'fn', 'tn', 'accuracy',
    'precision', 'recall', 'f1', 'roc_auc', 'pr_auc'}. Предсказание —
    вероятность не меньше порога. При одном классе в y_true площади
    равны None.

    Что чем меряется:
    - **precision** — какая доля тех, кого мы позвали, оказалась целевой.
      Дорого, когда контакт стоит денег;
    - **recall** — какую долю целевых мы нашли. Дорого, когда пропуск
      стоит денег;
    - **ROC-AUC** — качество ранжирования независимо от порога;
    - **PR-AUC** — то же, но информативнее на редком классе.

    На доле положительных 1% ROC-AUC 0.9 выглядит отлично, а PR-AUC
    может быть 0.1 — и это честная картина.
    """
    raise NotImplementedError


def accuracy_trap(y_true, y_proba, threshold=0.5):
    """Сравнение accuracy модели с accuracy болвана, отвечающего «нет».

    Возвращает {'model_accuracy', 'always_zero_accuracy', 'beats_dumb',
    'positive_rate', 'recall', 'note'}.

    На редком классе accuracy почти равна доле нулей. Модель с accuracy
    0.97 при доле положительных 3% может не находить вообще никого —
    и это тот самый случай, когда в отчёте пишут «точность 97%».

    Проверять надо recall: он мгновенно показывает, что модель не
    нашла ни одного целевого объекта.
    """
    raise NotImplementedError


def threshold_for_business(y_true, y_proba, cost_fp, cost_fn, gain_tp=0.0,
                           steps=101):
    """Порог, максимизирующий деньги, а не F1.

    Возвращает {'best', 'curve', 'default_value', 'note'}.
    best — словарь {'threshold', 'value', 'tp', 'fp', 'fn'} с
    максимальной ценностью; при равенстве берётся первый (меньший порог).
    curve — DataFrame по всем порогам от 0 до 1 включительно, steps штук.
    default_value — ценность при пороге 0.5.

    Ценность = gain_tp × TP − cost_fp × FP − cost_fn × FN.

    **Порог 0.5 — не значение по умолчанию, а произвольный выбор.** Он
    оптимален ровно тогда, когда ошибки стоят одинаково, а так почти
    никогда не бывает. Пропустить уходящего клиента с LTV 200 евро
    дороже, чем зря позвонить тому, кто и так остался.

    Это единственная задача спринта, где ответ считается в деньгах,
    и именно её результат идёт в разговор с бизнесом.
    """
    raise NotImplementedError


def regression_metrics(y_true, y_pred):
    """MAE, RMSE, MAPE, R² и отношение RMSE к MAE.

    Возвращает {'mae', 'rmse', 'mape', 'r2', 'rmse_to_mae', 'note'}.
    MAPE считается только по ненулевым истинным значениям; если таких
    нет — None. MAPE в процентах.

    Отношение RMSE к MAE — бесплатный диагноз. RMSE возводит ошибки в
    квадрат и потому чувствителен к выбросам: заметно больше единицы
    означает, что в ошибках есть крупные промахи, и стоит посмотреть
    на них отдельно, а не улучшать модель в среднем.

    MAPE удобна для разговора («ошибаемся на 12%»), но взрывается
    рядом с нулём и несимметрична: занижение и завышение штрафуются
    по-разному.
    """
    raise NotImplementedError


# === Часть 5. Пайплайн =====================================================

def build_pipeline(cat_cols, num_cols, model, scale=True):
    """Сборка ColumnTransformer и Pipeline.

    Возвращает Pipeline из двух шагов: 'pre' и 'model'.
    В 'pre' — ColumnTransformer с двумя ветками:
    'cat' — заполнение самым частым значением, затем OneHotEncoder с
    handle_unknown='ignore'; 'num' — заполнение медианой и, при
    scale, StandardScaler.

    **Пайплайн — не про красоту кода, а про корректность.** Без него
    препроцессинг считается на всей выборке: медиана, среднее для
    масштабирования, список категорий. Всё это подсматривает в
    тестовую часть, и кросс-валидация даёт завышенную оценку.

    Внутри пайплайна sklearn пересчитывает препроцессинг на каждом
    фолде заново — по обучающей части и только по ней.

    handle_unknown='ignore' нужен, потому что в тестовой части
    обязательно встретится категория, которой не было в обучающей.
    Без него код упадёт в проде через месяц после запуска.
    """
    raise NotImplementedError


def learning_curve_verdict(train_scores, val_scores, gap_threshold=0.1,
                           low_threshold=0.7):
    """Диагноз по качеству на обучении и на валидации.

    Возвращает {'train', 'val', 'gap', 'verdict', 'advice'} — средние
    по переданным спискам.

    | условие | вердикт |
    |---|---|
    | gap не меньше порога И train выше low_threshold | 'переобучение' |
    | и train, и val ниже low_threshold | 'недообучение' |
    | иначе | 'приемлемо' |

    advice — что делать; для 'приемлемо' пустая строка.

    Переобучение: модель выучила шум обучающей выборки. Лечится
    большим числом данных, регуляризацией, упрощением модели.
    Недообучение: модель слишком проста для задачи, и на обучении ей
    тоже плохо.

    Разрыв важнее абсолютных значений. Train 0.99 и val 0.72 — это
    провал, хотя первое число выглядит прекрасно.
    """
    raise NotImplementedError


def cv_score(pipeline, X, y, cv=5, scoring="roc_auc", seed=0):
    """Кросс-валидация со стратификацией и разбросом.

    Возвращает {'scores', 'mean', 'std', 'lo', 'hi', 'stable', 'note'}.
    lo и hi — среднее ± два стандартных отклонения.
    stable — стандартное отклонение меньше 0.05.
    Разбиение — StratifiedKFold с shuffle и random_state=seed.

    Стратификация нужна, чтобы доля редкого класса была одинаковой во
    всех фолдах: без неё на несбалансированных данных в каком-то фолде
    положительных может не оказаться вовсе.

    **Разброс важнее среднего.** 0.85 ± 0.01 и 0.85 ± 0.10 — разные
    результаты, и второй означает, что числу верить нельзя. Отчитываться
    одним средним по кросс-валидации — то же самое, что отчитываться
    средним без интервала в спринте 11.
    """
    raise NotImplementedError
