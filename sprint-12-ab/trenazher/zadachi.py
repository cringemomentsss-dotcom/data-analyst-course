"""
ТРЕНАЖЁР СПРИНТА 12. 18 задач на A/B-тестирование.

Половина задач — не про расчёт эффекта, а про валидацию и ловушки.
Это не случайно: посчитать z-тест умеет калькулятор, а понять, что
эксперимент невалиден, — нет.

Проверка:    make test SPRINT=12
Одна задача: python3 -m pytest sprint-12-ab/trenazher -q -k srm
"""

import numpy as np
import pandas as pd
from scipy import stats


# === Часть 3. Валидация до анализа =========================================

def srm_check(n_control, n_treatment, expected_share=0.5, alpha=0.001):
    """Проверка Sample Ratio Mismatch — расхождения размеров групп с планом.

    Возвращает {'n_control', 'n_treatment', 'share_treatment',
    'chi2', 'pvalue', 'srm_detected'}.

    Критерий — хи-квадрат с одной степенью свободы на фактических
    против ожидаемых размерах. Уровень по умолчанию 0.001, а не 0.05:
    SRM — проверка инфраструктуры, и ложная тревога здесь дешевле
    пропущенной поломки.

    expected_share — планируемая доля treatment. Не всегда 0.5:
    бывают запуски 90/10 и 95/5.

    Это ПЕРВОЕ, что делают с результатом эксперимента. Если SRM есть,
    остальной анализ не имеет смысла: рандомизация не сработала,
    и группы несравнимы по причинам, которых мы не видим.
    """
    raise NotImplementedError


def balance_check(share_control, share_treatment, n_control, n_treatment, alpha=0.01):
    """Сбалансированность групп по признаку, известному ДО эксперимента.

    Возвращает {'diff', 'z', 'pvalue', 'imbalanced'}.

    Проверяются доли устройств, стран, платформ, новых против старых.
    Рандомизация должна была уравнять их. Если не уравняла — либо
    бакетирование сломано, либо выборка мала.

    Важно: проверять можно только признаки, зафиксированные до
    попадания в эксперимент. Проверка по признакам, возникшим ПОСЛЕ
    (например, по числу покупок), бессмысленна: на них влияет сам тест.
    """
    raise NotImplementedError


# === Часть 4. Анализ результата ============================================

def ab_proportions(x_control, n_control, x_treatment, n_treatment, alpha=0.05):
    """Полный анализ конверсии в A/B-тесте.

    Возвращает {'p_control', 'p_treatment', 'abs_effect', 'rel_effect',
    'z', 'pvalue', 'ci_lo', 'ci_hi', 'significant'}, где ci — интервал
    АБСОЛЮТНОЙ разницы.

    Напоминание из спринта 11: в статистике знаменатель на объединённой
    доле, в интервале разницы — на необъединённых. Это разные формулы.

    Главное в результате — не p-value, а интервал эффекта. Он отвечает
    и на вопрос «есть ли эффект» (содержит ли ноль), и на вопрос
    «какой примерно», а второй для решения важнее.
    """
    raise NotImplementedError


def ab_means(control, treatment, alpha=0.05):
    """Анализ непрерывной метрики: средний чек, длительность, время.

    Возвращает {'mean_control', 'mean_treatment', 'abs_effect',
    'rel_effect', 'pvalue', 'ci_lo', 'ci_hi', 'significant'}.

    t-тест Уэлча. None в данных игнорировать.
    """
    raise NotImplementedError


def bootstrap_diff_ci(control, treatment, stat_fn=np.median, n_boot=2000,
                      conf=0.95, seed=42):
    """Бутстрап-интервал разницы для произвольной статистики.

    Возвращает {'point', 'ci_lo', 'ci_hi', 'significant'}, где
    significant — интервал не содержит нуля.

    Зачем в A/B: для медианы, перцентиля, ARPU и любых отношений
    аналитической формулы интервала разницы нет. А медиана в тестах
    на выручку часто честнее среднего, потому что не зависит от
    одного кита, случайно попавшего в тестовую группу.
    """
    raise NotImplementedError


# === Часть 2. Планирование =================================================

def mde_proportion(p, n_per_group, alpha=0.05, power=0.8):
    """Минимальный обнаружимый эффект. Считается ДО запуска."""
    raise NotImplementedError


def sample_size_proportion(p, mde, alpha=0.05, power=0.8):
    """Размер одной группы для обнаружения эффекта mde. Целое, вверх."""
    raise NotImplementedError


def experiment_duration(daily_units, p, mde, groups=2, alpha=0.05, power=0.8):
    """Сколько дней ждать.

    daily_units — сколько единиц рандомизации приходит в день.
    Возвращает {'n_per_group', 'n_total', 'days', 'weeks'}.

    Это ответ на вопрос продакта «сколько крутить тест» — цифрой,
    а не «ну недельку». И это же ответ на вопрос «а можем ли мы вообще
    это измерить»: если получилось четыре месяца, тест не нужен,
    нужен другой способ проверки.
    """
    raise NotImplementedError


# === Часть 5. Снижение дисперсии ===========================================

def cuped_theta(y, y_pre):
    """Коэффициент θ для CUPED: cov(y, y_pre) / var(y_pre)."""
    raise NotImplementedError


def cuped_adjust(y, y_pre, theta=None):
    """Скорректированная метрика: y − θ·(y_pre − mean(y_pre)).

    Если theta не передан — посчитать самому.

    Идея CUPED: если у нас есть та же метрика за период ДО эксперимента,
    можно вычесть предсказуемую часть разброса. Оставшаяся дисперсия
    меньше, значит тот же эффект обнаруживается на меньшей выборке —
    или тот же тест обнаруживает меньший эффект.

    Требование: среднее скорректированной метрики должно совпадать
    со средним исходной. CUPED снижает дисперсию, но не сдвигает оценку.
    """
    raise NotImplementedError


def cuped_variance_reduction(y, y_pre):
    """Насколько CUPED снизил дисперсию.

    Возвращает {'var_before', 'var_after', 'reduction_pct', 'theta'}.

    Проверь на двух случаях: когда предпериод сильно связан с метрикой
    (снижение может быть больше 50%) и когда не связан вовсе
    (снижение около нуля — и метод не должен вредить).
    """
    raise NotImplementedError


# === Часть 6. Ловушки ======================================================

def peeking_false_positive_rate(n_per_group, p, n_peeks, alpha=0.05,
                                trials=2000, seed=42):
    """Симуляция подглядывания.

    Провести trials экспериментов, в которых эффекта НЕТ (обе группы
    с одной и той же вероятностью p). По ходу каждого эксперимента
    проверять значимость n_peeks раз, в равномерно распределённых
    точках. Считать эксперимент «ложно значимым», если хотя бы одна
    проверка дала значимость.

    Возвращает {'n_peeks', 'nominal_alpha', 'actual_fpr'}.

    Что должно получиться: при одной проверке фактическая доля ложных
    срабатываний около alpha. При десяти — в четыре-пять раз больше.

    Это самый убедительный аргумент против «а давайте посмотрим,
    как идёт тест». Посчитай сам и запомни цифру.
    """
    raise NotImplementedError


def segment_analysis(df, segment_col, variant_col, success_col, alpha=0.05):
    """z-тест по каждому сегменту с поправкой на множественность.

    df — датафрейм со столбцами сегмента, варианта ('control'/'treatment')
    и бинарного успеха (0/1).

    Возвращает DataFrame со столбцами: segment, n_control, n_treatment,
    p_control, p_treatment, abs_effect, pvalue, significant_raw,
    significant_adjusted.

    Поправка — Бонферрони по числу сегментов. Без неё разбор по десяти
    сегментам почти гарантированно найдёт «значимый» эффект в одном
    из них, и это будет шум.
    """
    raise NotImplementedError


def simpson_check(overall_effect, segment_effects):
    """Проверка на Симпсонов парадокс.

    Возвращает {'overall_sign', 'segment_signs', 'paradox_detected'}.

    Парадокс — когда общий эффект одного знака, а ВСЕ сегменты
    другого. Если сегменты просто разные, это не парадокс,
    а гетерогенность эффекта, и трактуется иначе.
    """
    raise NotImplementedError
